# cPanel / Flask deployment deps (NOT streamlit — that can't run on shared cPanel).
# In the Python App: Configuration files -> add this filename -> Run Pip Install.
flask
requests
beautifulsoup4
lxml
rapidfuzz
python-docx
# Reads an UPLOADED résumé PDF back into text (core.resume_text_from_upload). Pure Python,
# ~294 KB, no compiled extensions — safe on shared cPanel. Imported lazily, so if this install
# is skipped the upload says "paste the text instead" rather than 500ing.
pypdf==6.15.0
# AI résumé tailoring + form-answering: Claude when ANTHROPIC_API_KEY is set (preferred), else Gemini.
# Both call their REST APIs via `requests` above — no SDK / extra package to install.
# python-docx powers Resume Brain's .docx export (degrades to .txt if absent).
# NOTE: PDF résumé export needs Tectonic on the server (scripts/get_tectonic.sh). Without it, PDF
# falls back to .docx (or .txt if python-docx is also missing).

# Postgres driver — only imported when PG_DSN is set (pgrest.py talks to a self-hosted
# database instead of Supabase PostgREST). Harmless when unset: the import is lazy.
psycopg2-binary>=2.9
