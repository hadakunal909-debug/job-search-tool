{
  "name": "job-search-tool",
  "version": "1.0.0",
  "private": true,
  "description": "Personal job-search tool. This is a PYTHON/Flask app — package.json only exists so `npm run dev` works out of habit; the scripts just call Python. The React islands under web/ have their own package.json.",
  "scripts": {
    "dev": "python web.py",
    "test": "python scripts/run_tests.py",
    "docs": "python scripts/build_docs.py",
    "streamlit": "python -m streamlit run app.py",
    "scrape": "python -m scraper",
    "score": "python -m scraper.score_jobs",
    "verify-dates": "python -m scraper.verify_dates",
    "refresh": "python -m scraper && python -m scraper.score_jobs && python -m scraper.verify_dates"
  },
  "dependencies": {
    "pdfjs-dist": "^6.2.108"
  }
}
