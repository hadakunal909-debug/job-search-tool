# Index — symptom to source

<!-- GENERATED by scripts/build_docs.py. Do not edit: your changes will be overwritten and CI will fail. The prose lives in the INDEX tuple in that script, keyed by symbol so the line numbers here are always current. -->

Start here when something is wrong. Every line number below is resolved from the code at generation time, so it is correct by construction rather than by anyone's diligence.

| | meaning |
|---|---|
| 🔴 | changing this alone breaks production **silently** |
| 🟡 | lives in two or more places that must agree |
| 🔵 | one place, safe to change, but non-obvious to find |

| | Symptom | Go here | Must stay in step with | Guard |
|---|---|---|---|---|
| 🔴 | The feed shows a job it shouldn't, or hides one it should | [`web.py:2145`](../web.py#L2145) `_filter_rows` -- the server-side filter | [`static/app.js:813`](../static/app.js#L813) `matches` (client) and [`core.py:2579`](../core.py#L2579) `prefs_match` (email digest). All three must agree. The switch is [`web.py:417`](../web.py#L417) `_FEED_INLINE_MAX` = 4000: below it the browser filters the whole corpus, above it the server does. | `python scripts/feed_parity.py` |
| 🟡 | An employer tile shows a monogram where it should show a logo | [`web.py:2571`](../web.py#L2571) `logo_url` -- resolves one same-origin path from static/logos/index.json | the asset is harvested by scripts/build_logos.py and judged on its PIXELS, not on its status code. Nothing is fetched from a third party at request time, and the CSP's img-src enforces that. A missing tile means the manifest and the directory disagree. | `python scripts/build_logos.py --check` |
| 🟡 | A field is missing from a feed card, or I added one | [`web.py:1306`](../web.py#L1306) `_build_row` -- one dict per card, and every key is consumed by the client | [`static/app.js:425`](../static/app.js#L425) `cardHTML`. ~16 function-for-function twins exist between these two files; this pair and the filter above are the ones that bite. | `python scripts/test_card_meta.py` |
| 🔵 | Search misses an obvious hit, or ranks badly | [`web.py:2085`](../web.py#L2085) `searchHit` and [`web.py:2109`](../web.py#L2109) `searchRank` -- typo-tolerant matching | mirrored in app.js function-for-function. [`web.py:2011`](../web.py#L2011) `_search_tol` sets how much typo slack a term gets by length. | `python scripts/test_search_and_similar.py` |
| 🔵 | The feed is slow, or shows stale jobs after a scrape | [`web.py:826`](../web.py#L826) `get_jobs` -- TTL cache over the corpus, and [`web.py:1741`](../web.py#L1741) `ranked_rows` | [`web.py:888`](../web.py#L888) `_invalidate_jobs` is what a write must call. The on-disk gzip snapshot is [`web.py:766`](../web.py#L766) `_snapshot_read` / [`web.py:780`](../web.py#L780) `_snapshot_write`, shared across Passenger workers. | `python scripts/test_jobs_cache.py` |
| 🟡 | The FIRST page load is slow and every one after it is fast | [`web.py:1630`](../web.py#L1630) `_base_rows` -- every card field except the score, built once per CORPUS | of the 41 keys [`web.py:1306`](../web.py#L1306) `_build_row` emits, exactly one -- score -- depends on who is asking, so [`web.py:1741`](../web.py#L1741) `ranked_rows` overlays that onto shallow copies of a shared list. Two rules hold it together. [`web.py:1487`](../web.py#L1487) `_dedupe_rows` must run AFTER the overlay, because [`web.py:1477`](../web.py#L1477) `_dupe_rank` tie-breaks on the score and folding duplicates while every base score is still 0 keeps a different copy. And the cache refuses to key on jobs_fingerprint()'s "don't know" answer, (None, ""), which is a NON-EMPTY and therefore truthy tuple -- [`web.py:888`](../web.py#L888) `_invalidate_jobs` clears it outright because the extension's JD patch moves neither half of that fingerprint. [`web.py:8981`](../web.py#L8981) `warm` builds the shared half off the user's path; [`web.py:397`](../web.py#L397) `_cache_max` charges it one entry. | `python scripts/test_speed_caches.py` |
| 🔴 | The first feed load after a scrape takes many seconds | [`web.py:9043`](../web.py#L9043) `_warm_user_scores` -- every account's score file, written off the request path | the shared half was never the dominant term here. Score files are keyed on (user, résumé) with the corpus fingerprint inside, so a scrape invalidates all of them and the next visitor paid a full pass over every row: 46% of live renders were over 2 s, median 5.0 s, one at 15.7 s. [`web.py:8981`](../web.py#L8981) `warm` writes them now, and it is safe on every keep-warm tick because a still-valid file short-circuits. The pass itself is ~4x cheaper too: [`core.py:573`](../core.py#L573) `_term_present` is memoised (76% of the pass, 96.8% hit rate) and [`core.py:719`](../core.py#L719) `score_pct` skips the have/missing sorts [`web.py:1074`](../web.py#L1074) `user_scores` throws away. score_pct MUST stay equal to [`core.py:670`](../core.py#L670) `score_against`[0] -- that is every match percentage in the product. | `python scripts/test_speed_caches.py` |
| 🔵 | "Similar roles" on a job page looks unrelated | [`web.py:1798`](../web.py#L1798) `_title_index` and [`web.py:1819`](../web.py#L1819) `_similar_roles` | [`web.py:1785`](../web.py#L1785) `_TITLE_STOP` is the rail's list and is deliberately SHORT -- seniority and level words are real signal and IDF already discounts them for being common. Do not confuse it with [`web.py:5016`](../web.py#L5016) `_USAGE_STOP`, which is /admin/usage's token-lift list and stops the opposite things on purpose. They shared a name until 2026-08-20; see B1. | `python scripts/test_search_and_similar.py` |
| 🔴 | A job exists on the employer's board but never reaches my feed | the keep loop in [`scraper/__init__.py:8556`](../scraper/__init__.py#L8556) `main` -- nine gates in order, each one tallied | the drop reason is printed at the end of a run. Read the count, then find that exact string in the funnel diagram in docs/ARCHITECTURE.md, which is generated from the same dict. [`core.py:2276`](../core.py#L2276) `admits_on_description` is the rescue path for a job whose TITLE matched nothing. | `python scripts/dump_titles.py` |
| 🟡 | A whole board suddenly returns nothing | the adapter for its ATS in [`scraper/__init__.py:6419`](../scraper/__init__.py#L6419) `SCRAPERS` -- 29 entries, ats_type -> function | [`scraper/__init__.py:1792`](../scraper/__init__.py#L1792) `SOURCES` holds the built-in boards; app-added ones come from the boards table and are merged on top. [`scraper/__init__.py:8318`](../scraper/__init__.py#L8318) `save_board_health` keeps an 8-run rolling window per board, visible in /admin/data. | `python scripts/verify_parsers.py` |
| 🔴 | Jobs are disappearing that should still be live | [`scraper/__init__.py:8435`](../scraper/__init__.py#L8435) `reconcile_closed` -- the 3-consecutive-miss rule | every guard around it moves together: CLOSED_AFTER_MISSES, RECONCILE_MIN_ROWS, RECONCILE_MIN_RATIO. Loosen one and a bot-walled board retires its whole inventory in a single pass. It sets is_active=false and never deletes. | `python test_liveness.py` |
| 🟡 | The corpus is growing or shrinking unexpectedly | [`scraper/__init__.py:2183`](../scraper/__init__.py#L2183) `MAX_AGE_DAYS` on the way in, [`db.py:1104`](../db.py#L1104) `prune_old_jobs` on the way out | MAX_AGE_DAYS, PRUNE_DAYS and [`db.py:1054`](../db.py#L1054) `AGE_LONG_DAYS` must move together. If the intake gate and the prune disagree, the corpus drifts to whichever is looser. | `python scripts/prune_stale.py --dry-run` |
| 🔵 | The same job appears twice | [`web.py:1487`](../web.py#L1487) `_dedupe_rows` for display, [`core.py:2379`](../core.py#L2379) `posting_key` for identity | [`scraper/__init__.py:2457`](../scraper/__init__.py#L2457) `canonical_url` runs before insert, so the writer and the table cannot disagree about the key. Employer floods are GROUPED, never deduped -- that is deliberate. | `python test_canonical_url.py` |
| 🔵 | Adding a board from the app says it can't detect the ATS | [`scraper/__init__.py:6585`](../scraper/__init__.py#L6585) `detect_board` -- the detect chain, then [`scraper/__init__.py:7026`](../scraper/__init__.py#L7026) `probe_board` which validates before any insert | digitas is deliberately NOT emitted by detect_board: it reads a 14 MB sitemap and must not be pointable at an arbitrary host. | `python scripts/verify_parsers.py` |
| 🟡 | Adding a board from the EXTENSION says "Couldn't add" and no reason | [`web.py:8762`](../web.py#L8762) `ext_detect_board` -- the extension's half of /add-board | the two must agree, and for months they did not: this route refused on a falsy probe_board count and returned no `error` at all, so popup.js printed the same generic hint for every failure it has. It also re-ran the whole detect chain on the ADD click, half of which is a live fetch, and it stored [`scraper/__init__.py:6585`](../scraper/__init__.py#L6585) `detect_board`'s name suggestion verbatim -- see [`scraper/__init__.py:6548`](../scraper/__init__.py#L6548) `name_is_sluglike` and [`scraper/__init__.py:6508`](../scraper/__init__.py#L6508) `board_display_name`, the guards /add-board already used. | `python scripts/test_add_board_api.py` |
| 🟡 | Match percentages look wrong across the board | [`core.py:670`](../core.py#L670) `score_against` and [`core.py:630`](../core.py#L630) `core_terms` | [`core.py:627`](../core.py#L627) `CORE_WEIGHT_FRACTION` weights the terms that matter. If you change what the score MEANS, bump [`core.py:2438`](../core.py#L2438) `MIN_SCALE` or every saved search keeps filtering on the old scale. Do not make the number relative -- see the note in CLAUDE.md. | `python test_scoring.py` |
| 🔴 | A new scoring field isn't persisting | [`scraper/score_jobs.py:1009`](../scraper/score_jobs.py#L1009) `_persist_derived` | [`db.py:536`](../db.py#L536) `COLS_SCORE` must contain every column the diff compares, or have.get(k) is None forever and every row re-upserts on all three runs a day. | `python test_scoring.py` |
| 🔴 | Scores changed and nobody touched the scorer | idf.json, written by [`core.py:303`](../core.py#L303) `save_idf` on the FULL pass only | a partial rebuild silently re-weights every score in the corpus. It is committed source-of-truth, not a cache. [`core.py:313`](../core.py#L313) `load_idf` is every reader. | `python test_scoring.py` |
| 🟡 | A job page shows no description although we fetched one | [`scraper/score_jobs.py:1389`](../scraper/score_jobs.py#L1389) `_jd_corpus` -- the two-store reconciliation | jd_cache.json.gz and the jobs.jd column are written at different moments and HAVE diverged, to 48% of pending rows. Any new writer of a description must touch both. | `python test_jd_persist.py` |
| 🔵 | A description is present but useless -- a cookie banner, a nav bar | [`scraper/score_jobs.py:1173`](../scraper/score_jobs.py#L1173) `_is_thin_jd` and the per-host retry ledger around it | three different thresholds exist: [`core.py:375`](../core.py#L375) `_MIN_JD_CHARS`, score_jobs' own page minimum, and the thin test. Editing score_jobs.py at all reopens every backed-off host for one round, because the ledger key hashes the file. | `python scripts/refetch_thin_jds.py --dry-run` |
| 🔴 | jd_terms round-trips wrong, or every row re-upserts every run | [`core.py:822`](../core.py#L822) `pack_analyzed` / [`core.py:834`](../core.py#L834) `unpack_analyzed` | jobs.jd_terms is TEXT, not jsonb, on purpose, and the KEY ORDER is semantic: it is analyze_jd's frozen term order, it breaks ties in the skill panel, and score_jobs diffs the stored string to decide whether to write at all. | `python test_jd_persist.py` |
| 🔴 | A scrape reported success but the database did not change | [`db.py:46`](../db.py#L46) `_LazyHTTP` -- which of the four backends you actually got | .env is read relative to the CURRENT WORKING DIRECTORY, so a script that does not cd here first silently writes somewhere else and exits 0. [`db.py:79`](../db.py#L79) `_check_backend_intent` plus DB_REQUIRE is how you make that fail loudly. [`db.py:280`](../db.py#L280) `backend_name` tells you which backend you have; has_remote_db() answers True for three of them and will not. | `python scripts/probe_db_proxy.py` |
| 🟡 | A query works locally but 403s from GitHub Actions | [`dbproxy.py:53`](../dbproxy.py#L53) `ALLOWED_TABLES` -- the proxy's allowlist | it must track db.py's table constants. A new table works on PG_DSN and is refused through the proxy, so the failure only ever appears in CI. | `python scripts/test_dbproxy.py` |
| 🔴 | A script hangs, or the egress bill jumps | [`db.py:598`](../db.py#L598) `load_jobs` called with no cols -- that is ~168 MB of descriptions at 26k rows | [`db.py:565`](../db.py#L565) `_warn_full_jd_read` prints the caller; it exists because four scripts did it. Also [`db.py:326`](../db.py#L326) `_fetch_all` OVERWRITES limit with its 1000-row page size, so asking it for one row walks the whole table -- copy [`db.py:2810`](../db.py#L2810) `newest_event_ts` for a one-row select. | `python scripts/probe_db_proxy.py` |
| 🔴 | I DID pass cols= and it still read the whole corpus | [`db.py:598`](../db.py#L598) `load_jobs` again -- the narrow select is best-effort and falls back SILENTLY | ONE column name that does not exist on the table fails the entire select, and the fallback reads every description. The only signal is [`db.py:565`](../db.py#L565) `_warn_full_jd_read` on stderr, which is easy to scroll past. DERIVED FIELDS ARE THE TRAP: role_track looks like a column and is computed by [`core.py:1992`](../core.py#L1992) `role_track` per request, so asking for it costs a full-corpus read -- that is how this row got written. Prefer a [`db.py:536`](../db.py#L536) `COLS_SCORE`-style constant, or check one name first with db._fetch_all(db.TABLE, {'select': name, 'limit': 1}). | `python scripts/dump_schema.py` |
| 🔵 | A column exists in code but not in the live database | schema.sql, regenerated by asking Postgres to describe itself | [`db.py:194`](../db.py#L194) `JOBS_DERIVED_SQL` describes the same table a second time. resume_files and brain_companies are in db.py and the proxy allowlist but absent from schema.sql -- brain_companies deliberately, see the admin health check. | `python scripts/dump_schema.py` |
| 🔵 | Text renders in a fallback face, or a font is refused in the console | the @font-face block at the top of static/style.css -- six woff2 in static/fonts/ | the fonts are OURS, exactly like the logos: nothing is fetched from Google at request time and [`web.py:224`](../web.py#L224) `_CSP_TEMPLATE` says font-src 'self' with no remote origin left in style-src either, so a re-added <link> to fonts.googleapis.com fails visibly instead of quietly putting two third-party handshakes in front of first paint. A preload href in templates/base.html must equal its @font-face src BYTE FOR BYTE, ?v= included, or the file is fetched twice; and ?v= is also what earns the immutable Cache-Control. | `python scripts/test_fonts.py` |
| 🔵 | Load more, a tab switch or a filter change gives no feedback | [`static/app.js:891`](../static/app.js#L891) `skeletonHTML`, [`static/app.js:902`](../static/app.js#L902) `animateIn` and [`static/app.js:913`](../static/app.js#L913) `moreLoading` | skeletonHTML is the twin of the eight .skel tiles in templates/_feedgrid.html, which the server paints once and the first render then wipes for good. animateIn is called with the index of the first NEW card, so a Load more animates the page it appended rather than re-flashing the feed, and its delay is capped -- uncapped, card 60 waits 1.1 s. moreLoading must be cleared on EVERY exit from [`static/app.js:1315`](../static/app.js#L1315) `renderServer`, including the 429 and 401 branches, or a failed page leaves the button spinning. Any new animation needs its own prefers-reduced-motion rule: the global one in style.css clamps animation-duration and says nothing about animation-DELAY. | `python scripts/run_tests.py --only card_meta` |
| 🟡 | A POST returns 400, or a new form silently fails | [`web.py:7268`](../web.py#L7268) `_require_csrf` -- runs before EVERY cookie-authenticated write | [`web.py:7256`](../web.py#L7256) `_CSRF_EXEMPT` has four entries and each carries its justification. New routes are protected by default; adding to that set needs the same standard of reasoning. | `python scripts/smoke_app.py` |
| 🔴 | Analytics numbers look inflated, or a test wrote real events | [`analytics.py:187`](../analytics.py#L187) `emit` and the auto page_view in [`web.py:331`](../web.py#L331) `_ev_page_view` | EV_OFF=1 must be in the environment BEFORE anything imports web -- analytics.py reads it once, at import. One unguarded parity run wrote 98.8% of all recorded feed_view events. scripts/run_tests.py sets it per subprocess so this cannot recur. | `python scripts/run_tests.py --only card_meta` |
| 🟡 | The Chrome extension stopped working, or says it is out of date | [`web.py:8128`](../web.py#L8128) `EXT_MIN_VERSION` -- the server's floor | extension/manifest.json's version must match, and NOTHING enforces that. Bumping the manifest alone tells every install it is current. The response carries no update_url on purpose -- the extension is side-loaded, so Chrome can never update it; the `how` string tells the user to pull and reload, and extension/popup.js is what shows it. | `python scripts/test_ext_contract.py` |
| 🟡 | An aggregator relist reached the apply queue | [`web.py:8347`](../web.py#L8347) `_QUEUE_SKIP_HOSTS` | a verbatim retyped copy of [`core.py:2305`](../core.py#L2305) `AGGREGATOR_HOSTS`. Adding a sixth aggregator updates the feed's dedupe and misses the queue. No test covers this yet. | *none — write one* |
| 🔵 | A job description renders badly -- lost headings, mangled lists | [`jdrender.py:666`](../jdrender.py#L666) `render_jd` -- typed nodes, not markup, then one HTML pass | byte-frozen against scripts/fixtures/jd_html_baseline.json. Regenerating that baseline needs a reviewed diff; it is a snapshot, so it will happily bless a regression. | `python scripts/test_jdrender.py` |
| 🔵 | The resume grader scores something oddly | [`resume_score.py:1340`](../resume_score.py#L1340) `score_resume` -- ~25 checks behind a weight table | [`resume_keywords.py:173`](../resume_keywords.py#L173) `evaluate` owns the Skills category and decides presence via core's term matcher, so it agrees with the feed's match %. Voice rules are shared with the AI rewriter, so the grader and the rewriter cannot disagree about a filler word. | `python test_resume_score.py` |
| 🔵 | Sponsorship colour or tier looks wrong on a card | [`core.py:1275`](../core.py#L1275) `visa_tags` for the posting's own claim, [`core.py:1123`](../core.py#L1123) `sponsor_strength` for federal history | absence of a record is NOT evidence of non-sponsorship, and the feed is never filtered on it. [`core.py:946`](../core.py#L946) `is_cap_exempt` is the separate universities-and-hospitals path. | `python test_sponsor_flag.py` |
| 🔵 | A management role shows in a dev search, or vice versa | [`core.py:1992`](../core.py#L1992) `role_track` -- management wins ties, deliberately | [`core.py:1971`](../core.py#L1971) `roles_match` and ROLE_FAMILIES drive the role picker. [`core.py:2207`](../core.py#L2207) `reads_like_pm` is the description-level rule with its own anchor/point gates. | `python test_pm_rule.py` |
| 🔵 | grep or ripgrep finds nothing in static/app.js | a raw NUL byte at offset 46372 (line 733) makes ripgrep classify the file as binary and return no line numbers; git marks it -text for the same reason | use `grep -a`, `git grep`, or the app.js section of docs/MAP.md. It is the second-most-coupled file in the repo, so this matters more than it sounds. | *none — write one* |

## Known-wrong

Things that are broken or misleading right now. A symptom above may point here instead of at a fix — that is deliberate, because an index you can't trust is worse than no index.

### B1 — _TITLE_STOP was defined twice and the live copy was the wrong one (FIXED 2026-08-20)

[`web.py:1785`](../web.py#L1785) `_TITLE_STOP` was bound once for the similar-roles title index and again, 2,500 lines later, with a different word list for /admin/usage. Python resolves globals at call time, so [`web.py:1793`](../web.py#L1793) `_title_tokens` used the SECOND list -- which strips engineer, manager, analyst, developer, senior and lead, exactly the tokens that distinguish one role from another, and keeps job, role, remote, hybrid and usa, which the intended list removes. Measured effect: "Senior Project Manager, Remote - USA" matched on {project, remote, usa}, so it was ranked similar to any remote US job; it now matches on {manager, project, senior}. The first definition had no effect at all.

**Workaround:** Fixed by renaming the admin one to [`web.py:5016`](../web.py#L5016) `_USAGE_STOP`. Both lists were individually correct and self-documenting -- only the shared name was wrong. build_docs.py now detects module-level shadowing on every run and tells a real shadow apart from a deliberate compile-in-place rebind, so this class cannot return silently.

### B2 — /extension was advertised as the update URL and 404'd (FIXED 2026-08-20)

[`web.py:8150`](../web.py#L8150) `ext_version` returned request.host_url + "/extension" to every extension that checked its version, and no route serves that path. Three things were wrong at once: the URL 404'd, extension/popup.js never read the field (it shows the `how` string), and [`web.py:8128`](../web.py#L8128) `EXT_MIN_VERSION`'s own comment 40 lines above says the extension is side-loaded unpacked with "no update_url" precisely because Chrome can never update it. The field contradicted the design it sat next to.

**Workaround:** Fixed by removing the field rather than adding a page nobody would visit. `how` was already the real answer and was already being displayed.

### B3 — Two test suites could not fail (FIXED 2026-08-20)

scripts/test_visa.py and scripts/test_notify.py printed "N FAILED" and then exited 0. Both run in python-tests.yml under `set -e`, so neither could ever have failed a build. Verified with a forced failure: identical stdout, exit 0 before and exit 1 after.

**Workaround:** Fixed. scripts/run_tests.py also treats a FAILED line as failure regardless of exit code, because the bug class is cheap to keep catching.

### B4 — GitHub Actions cannot reach the database

The Actions DB_PROXY_SECRET has not matched the server's since roughly 2026-08-15, so scheduled runs die on HTTP 401 {"error":"bad signature"}. verify_dates, the analytics rollup and the digest email have not run there since.

**Workaround:** Repost detection is duplicated into bin/cron_scrape.sh and THAT copy works -- do not "clean up the duplicate". Fix by resetting the repository secret to match .db_proxy_secret.

### B5 — Seven employers have no board at all

ASML, Deutsche Bank, LTIMindtree, Marlabs, Qualcomm, Renesas and Tradeweb were reachable only through Adzuna, which was removed on 2026-08-16. Their boards rows still say ats_type="adzuna" and are inert.

**Workaround:** Each needs its real ATS tenant id; guessed Workday and Greenhouse tenants did not resolve. `python scripts/probe_adzuna_replacements.py` walks the detect chain.

## Shadowed definitions

Bound more than once at module level. Python resolves globals at call time, so the **last** binding is the one that runs — even if the code using it sits thousands of lines above. Found automatically, which is how B1 stopped being invisible.

*Rebind* means the second binding consumes the first (`X = tuple(f(p) for p in X)`) — deliberate, nothing lost. *Shadow* means it does not, so the earlier definition is dead code and whoever wrote it did not get what they intended.

| File | Name | Bound at | |
|---|---|---|---|
| `web.py` | `_BOARDS_TTL` | [4863](../web.py#L4863), [6657](../web.py#L6657) | ⚠ **shadow** |
| `resume_bullets.py` | `_METRIC_HINTS` | [75](../resume_bullets.py#L75), [113](../resume_bullets.py#L113) | rebind |

---

Full symbol map: [MAP.md](MAP.md) · How it works: [ARCHITECTURE.md](ARCHITECTURE.md) · How to do things: [OPERATIONS.md](OPERATIONS.md)
