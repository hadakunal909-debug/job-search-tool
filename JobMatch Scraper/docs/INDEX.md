# Index — symptom to source

<!-- GENERATED by scripts/build_docs.py. Do not edit: your changes will be overwritten and CI will fail. The prose lives in the INDEX tuple in that script, keyed by symbol so the line numbers here are always current. -->

Start here when something is wrong. Every line number below is resolved from the code at generation time, so it is correct by construction rather than by anyone's diligence.

| | meaning |
|---|---|
| 🔴 | changing this alone breaks production **silently** |
| 🟡 | lives in two or more places that must agree |
| 🔵 | one place, safe to change, but non-obvious to find |

| | Symptom | Go here | Must stay in step with | Guard |
|---|---|---|---|---|
| 🔴 | The feed shows a job it shouldn't, or hides one it should | [`web.py:1820`](../web.py#L1820) `_filter_rows` -- the server-side filter | [`static/app.js:804`](../static/app.js#L804) `matches` (client) and [`core.py:2390`](../core.py#L2390) `prefs_match` (email digest). All three must agree. The switch is [`web.py:366`](../web.py#L366) `_FEED_INLINE_MAX` = 4000: below it the browser filters the whole corpus, above it the server does. | `python scripts/feed_parity.py` |
| 🟡 | An employer tile shows a monogram where it should show a logo | [`web.py:2160`](../web.py#L2160) `logo_url` -- resolves one same-origin path from static/logos/index.json | the asset is harvested by scripts/build_logos.py and judged on its PIXELS, not on its status code. Nothing is fetched from a third party at request time, and the CSP's img-src enforces that. A missing tile means the manifest and the directory disagree. | `python scripts/build_logos.py --check` |
| 🟡 | A field is missing from a feed card, or I added one | [`web.py:1219`](../web.py#L1219) `_build_row` -- one dict per card, and every key is consumed by the client | [`static/app.js:400`](../static/app.js#L400) `cardHTML`. ~16 function-for-function twins exist between these two files; this pair and the filter above are the ones that bite. | `python scripts/test_card_meta.py` |
| 🔵 | Search misses an obvious hit, or ranks badly | [`web.py:1760`](../web.py#L1760) `searchHit` and [`web.py:1784`](../web.py#L1784) `searchRank` -- typo-tolerant matching | mirrored in app.js function-for-function. [`web.py:1686`](../web.py#L1686) `_search_tol` sets how much typo slack a term gets by length. | `python scripts/test_search_and_similar.py` |
| 🔵 | The feed is slow, or shows stale jobs after a scrape | [`web.py:754`](../web.py#L754) `get_jobs` -- TTL cache over the corpus, and [`web.py:1427`](../web.py#L1427) `ranked_rows` | [`web.py:816`](../web.py#L816) `_invalidate_jobs` is what a write must call. The on-disk gzip snapshot is [`web.py:710`](../web.py#L710) `_snapshot_read` / [`web.py:724`](../web.py#L724) `_snapshot_write`, shared across Passenger workers. | `python scripts/test_jobs_cache.py` |
| 🔵 | "Similar roles" on a job page looks unrelated | [`web.py:1474`](../web.py#L1474) `_title_index` and [`web.py:1495`](../web.py#L1495) `_similar_roles` | [`web.py:1461`](../web.py#L1461) `_TITLE_STOP` is the rail's list and is deliberately SHORT -- seniority and level words are real signal and IDF already discounts them for being common. Do not confuse it with [`web.py:4291`](../web.py#L4291) `_USAGE_STOP`, which is /admin/usage's token-lift list and stops the opposite things on purpose. They shared a name until 2026-08-20; see B1. | `python scripts/test_search_and_similar.py` |
| 🔴 | A job exists on the employer's board but never reaches my feed | the keep loop in [`scraper/__init__.py:8083`](../scraper/__init__.py#L8083) `main` -- nine gates in order, each one tallied | the drop reason is printed at the end of a run. Read the count, then find that exact string in the funnel diagram in docs/ARCHITECTURE.md, which is generated from the same dict. [`core.py:2106`](../core.py#L2106) `admits_on_description` is the rescue path for a job whose TITLE matched nothing. | `python scripts/dump_titles.py` |
| 🟡 | A whole board suddenly returns nothing | the adapter for its ATS in [`scraper/__init__.py:6094`](../scraper/__init__.py#L6094) `SCRAPERS` -- 29 entries, ats_type -> function | [`scraper/__init__.py:1783`](../scraper/__init__.py#L1783) `SOURCES` holds the built-in boards; app-added ones come from the boards table and are merged on top. [`scraper/__init__.py:7845`](../scraper/__init__.py#L7845) `save_board_health` keeps an 8-run rolling window per board, visible in /admin/data. | `python scripts/verify_parsers.py` |
| 🔴 | Jobs are disappearing that should still be live | [`scraper/__init__.py:7962`](../scraper/__init__.py#L7962) `reconcile_closed` -- the 3-consecutive-miss rule | every guard around it moves together: CLOSED_AFTER_MISSES, RECONCILE_MIN_ROWS, RECONCILE_MIN_RATIO. Loosen one and a bot-walled board retires its whole inventory in a single pass. It sets is_active=false and never deletes. | `python test_liveness.py` |
| 🟡 | The corpus is growing or shrinking unexpectedly | [`scraper/__init__.py:2174`](../scraper/__init__.py#L2174) `MAX_AGE_DAYS` on the way in, [`db.py:1101`](../db.py#L1101) `prune_old_jobs` on the way out | MAX_AGE_DAYS, PRUNE_DAYS and [`db.py:1051`](../db.py#L1051) `AGE_LONG_DAYS` must move together. If the intake gate and the prune disagree, the corpus drifts to whichever is looser. | `python scripts/prune_stale.py --dry-run` |
| 🔵 | The same job appears twice | [`web.py:1399`](../web.py#L1399) `_dedupe_rows` for display, [`core.py:2209`](../core.py#L2209) `posting_key` for identity | [`scraper/__init__.py:2371`](../scraper/__init__.py#L2371) `canonical_url` runs before insert, so the writer and the table cannot disagree about the key. Employer floods are GROUPED, never deduped -- that is deliberate. | `python test_canonical_url.py` |
| 🔵 | Adding a board from the app says it can't detect the ATS | [`scraper/__init__.py:6240`](../scraper/__init__.py#L6240) `detect_board` -- the detect chain, then [`scraper/__init__.py:6662`](../scraper/__init__.py#L6662) `probe_board` which validates before any insert | digitas is deliberately NOT emitted by detect_board: it reads a 14 MB sitemap and must not be pointable at an arbitrary host. | `python scripts/verify_parsers.py` |
| 🟡 | Adding a board from the EXTENSION says "Couldn't add" and no reason | [`web.py:7710`](../web.py#L7710) `ext_detect_board` -- the extension's half of /add-board | the two must agree, and for months they did not: this route refused on a falsy probe_board count and returned no `error` at all, so popup.js printed the same generic hint for every failure it has. It also re-ran the whole detect chain on the ADD click, half of which is a live fetch, and it stored [`scraper/__init__.py:6240`](../scraper/__init__.py#L6240) `detect_board`'s name suggestion verbatim -- see [`scraper/__init__.py:6222`](../scraper/__init__.py#L6222) `name_is_sluglike` and [`scraper/__init__.py:6182`](../scraper/__init__.py#L6182) `board_display_name`, the guards /add-board already used. | `python scripts/test_add_board_api.py` |
| 🟡 | Match percentages look wrong across the board | [`core.py:606`](../core.py#L606) `score_against` and [`core.py:566`](../core.py#L566) `core_terms` | [`core.py:563`](../core.py#L563) `CORE_WEIGHT_FRACTION` weights the terms that matter. If you change what the score MEANS, bump [`core.py:2268`](../core.py#L2268) `MIN_SCALE` or every saved search keeps filtering on the old scale. Do not make the number relative -- see the note in CLAUDE.md. | `python test_scoring.py` |
| 🔴 | A new scoring field isn't persisting | [`scraper/score_jobs.py:960`](../scraper/score_jobs.py#L960) `_persist_derived` | [`db.py:561`](../db.py#L561) `COLS_SCORE` must contain every column the diff compares, or have.get(k) is None forever and every row re-upserts on all three runs a day. | `python test_scoring.py` |
| 🔴 | Scores changed and nobody touched the scorer | idf.json, written by [`core.py:250`](../core.py#L250) `save_idf` on the FULL pass only | a partial rebuild silently re-weights every score in the corpus. It is committed source-of-truth, not a cache. [`core.py:260`](../core.py#L260) `load_idf` is every reader. | `python test_scoring.py` |
| 🟡 | A job page shows no description although we fetched one | [`scraper/score_jobs.py:1340`](../scraper/score_jobs.py#L1340) `_jd_corpus` -- the two-store reconciliation | jd_cache.json.gz and the jobs.jd column are written at different moments and HAVE diverged, to 48% of pending rows. Any new writer of a description must touch both. | `python test_jd_persist.py` |
| 🔵 | A description is present but useless -- a cookie banner, a nav bar | [`scraper/score_jobs.py:1124`](../scraper/score_jobs.py#L1124) `_is_thin_jd` and the per-host retry ledger around it | three different thresholds exist: [`core.py:322`](../core.py#L322) `_MIN_JD_CHARS`, score_jobs' own page minimum, and the thin test. Editing score_jobs.py at all reopens every backed-off host for one round, because the ledger key hashes the file. | `python scripts/refetch_thin_jds.py --dry-run` |
| 🔴 | jd_terms round-trips wrong, or every row re-upserts every run | [`core.py:719`](../core.py#L719) `pack_analyzed` / [`core.py:731`](../core.py#L731) `unpack_analyzed` | jobs.jd_terms is TEXT, not jsonb, on purpose, and the KEY ORDER is semantic: it is analyze_jd's frozen term order, it breaks ties in the skill panel, and score_jobs diffs the stored string to decide whether to write at all. | `python test_jd_persist.py` |
| 🔴 | A scrape reported success but the database did not change | [`db.py:59`](../db.py#L59) `_LazyHTTP` -- which of the four backends you actually got | .env is read relative to the CURRENT WORKING DIRECTORY, so a script that does not cd here first silently writes somewhere else and exits 0. [`db.py:80`](../db.py#L80) `_check_backend_intent` plus DB_REQUIRE is how you make that fail loudly. [`db.py:310`](../db.py#L310) `backend_name` tells you which backend you have; using_supabase() answers True for three of them and will not. | `python scripts/probe_db_proxy.py` |
| 🟡 | A query works locally but 403s from GitHub Actions | [`dbproxy.py:53`](../dbproxy.py#L53) `ALLOWED_TABLES` -- the proxy's allowlist | it must track db.py's table constants. A new table works on PG_DSN and is refused through the proxy, so the failure only ever appears in CI. | `python scripts/test_dbproxy.py` |
| 🔴 | A script hangs, or the egress bill jumps | [`db.py:623`](../db.py#L623) `load_jobs` called with no cols -- that is ~168 MB of descriptions at 26k rows | [`db.py:590`](../db.py#L590) `_warn_full_jd_read` prints the caller; it exists because four scripts did it. Also [`db.py:351`](../db.py#L351) `_fetch_all` OVERWRITES limit with its 1000-row page size, so asking it for one row walks the whole table -- copy [`db.py:2743`](../db.py#L2743) `newest_event_ts` for a one-row select. | `python scripts/probe_db_proxy.py` |
| 🔴 | I DID pass cols= and it still read the whole corpus | [`db.py:623`](../db.py#L623) `load_jobs` again -- the narrow select is best-effort and falls back SILENTLY | ONE column name that does not exist on the table fails the entire select, and the fallback reads every description. The only signal is [`db.py:590`](../db.py#L590) `_warn_full_jd_read` on stderr, which is easy to scroll past. DERIVED FIELDS ARE THE TRAP: role_track looks like a column and is computed by [`core.py:1822`](../core.py#L1822) `role_track` per request, so asking for it costs a full-corpus read -- that is how this row got written. Prefer a [`db.py:561`](../db.py#L561) `COLS_SCORE`-style constant, or check one name first with db._fetch_all(db.TABLE, {'select': name, 'limit': 1}). | `python scripts/dump_schema.py` |
| 🔵 | A column exists in code but not in the live database | schema.sql, regenerated by asking Postgres to describe itself | [`db.py:183`](../db.py#L183) `JOBS_DERIVED_SQL` describes the same table a second time. resume_files and brain_companies are in db.py and the proxy allowlist but absent from schema.sql -- brain_companies deliberately, see the admin health check. | `python scripts/dump_schema.py` |
| 🟡 | A POST returns 400, or a new form silently fails | [`web.py:6311`](../web.py#L6311) `_require_csrf` -- runs before EVERY cookie-authenticated write | [`web.py:6299`](../web.py#L6299) `_CSRF_EXEMPT` has four entries and each carries its justification. New routes are protected by default; adding to that set needs the same standard of reasoning. | `python scripts/smoke_app.py` |
| 🔴 | Analytics numbers look inflated, or a test wrote real events | [`analytics.py:187`](../analytics.py#L187) `emit` and the auto page_view in [`web.py:302`](../web.py#L302) `_ev_page_view` | EV_OFF=1 must be in the environment BEFORE anything imports web -- analytics.py reads it once, at import. One unguarded parity run wrote 98.8% of all recorded feed_view events. scripts/run_tests.py sets it per subprocess so this cannot recur. | `python scripts/run_tests.py --only card_meta` |
| 🟡 | The Chrome extension stopped working, or says it is out of date | [`web.py:7090`](../web.py#L7090) `EXT_MIN_VERSION` -- the server's floor | extension/manifest.json's version must match, and NOTHING enforces that. Bumping the manifest alone tells every install it is current. The response carries no update_url on purpose -- the extension is side-loaded, so Chrome can never update it; the `how` string tells the user to pull and reload, and extension/popup.js is what shows it. | `python scripts/test_ext_contract.py` |
| 🟡 | An aggregator relist reached the apply queue | [`web.py:7309`](../web.py#L7309) `_QUEUE_SKIP_HOSTS` | a verbatim retyped copy of [`core.py:2135`](../core.py#L2135) `AGGREGATOR_HOSTS`. Adding a sixth aggregator updates the feed's dedupe and misses the queue. No test covers this yet. | *none — write one* |
| 🔵 | A job description renders badly -- lost headings, mangled lists | [`jdrender.py:666`](../jdrender.py#L666) `render_jd` -- typed nodes, not markup, then one HTML pass | byte-frozen against scripts/fixtures/jd_html_baseline.json. Regenerating that baseline needs a reviewed diff; it is a snapshot, so it will happily bless a regression. | `python scripts/test_jdrender.py` |
| 🔵 | The resume grader scores something oddly | [`resume_score.py:1340`](../resume_score.py#L1340) `score_resume` -- ~25 checks behind a weight table | [`resume_keywords.py:173`](../resume_keywords.py#L173) `evaluate` owns the Skills category and decides presence via core's term matcher, so it agrees with the feed's match %. Voice rules are shared with the AI rewriter, so the grader and the rewriter cannot disagree about a filler word. | `python test_resume_score.py` |
| 🔵 | Sponsorship colour or tier looks wrong on a card | [`core.py:1129`](../core.py#L1129) `visa_tags` for the posting's own claim, [`core.py:977`](../core.py#L977) `sponsor_strength` for federal history | absence of a record is NOT evidence of non-sponsorship, and the feed is never filtered on it. [`core.py:843`](../core.py#L843) `is_cap_exempt` is the separate universities-and-hospitals path. | `python test_sponsor_flag.py` |
| 🔵 | A management role shows in a dev search, or vice versa | [`core.py:1822`](../core.py#L1822) `role_track` -- management wins ties, deliberately | [`core.py:1801`](../core.py#L1801) `roles_match` and ROLE_FAMILIES drive the role picker. [`core.py:2037`](../core.py#L2037) `reads_like_pm` is the description-level rule with its own anchor/point gates. | `python test_pm_rule.py` |
| 🔵 | grep or ripgrep finds nothing in static/app.js | a raw NUL byte at offset 46372 (line 733) makes ripgrep classify the file as binary and return no line numbers; git marks it -text for the same reason | use `grep -a`, `git grep`, or the app.js section of docs/MAP.md. It is the second-most-coupled file in the repo, so this matters more than it sounds. | *none — write one* |

## Known-wrong

Things that are broken or misleading right now. A symptom above may point here instead of at a fix — that is deliberate, because an index you can't trust is worse than no index.

### B1 — _TITLE_STOP was defined twice and the live copy was the wrong one (FIXED 2026-08-20)

[`web.py:1461`](../web.py#L1461) `_TITLE_STOP` was bound once for the similar-roles title index and again, 2,500 lines later, with a different word list for /admin/usage. Python resolves globals at call time, so [`web.py:1469`](../web.py#L1469) `_title_tokens` used the SECOND list -- which strips engineer, manager, analyst, developer, senior and lead, exactly the tokens that distinguish one role from another, and keeps job, role, remote, hybrid and usa, which the intended list removes. Measured effect: "Senior Project Manager, Remote - USA" matched on {project, remote, usa}, so it was ranked similar to any remote US job; it now matches on {manager, project, senior}. The first definition had no effect at all.

**Workaround:** Fixed by renaming the admin one to [`web.py:4291`](../web.py#L4291) `_USAGE_STOP`. Both lists were individually correct and self-documenting -- only the shared name was wrong. build_docs.py now detects module-level shadowing on every run and tells a real shadow apart from a deliberate compile-in-place rebind, so this class cannot return silently.

### B2 — /extension was advertised as the update URL and 404'd (FIXED 2026-08-20)

[`web.py:7112`](../web.py#L7112) `ext_version` returned request.host_url + "/extension" to every extension that checked its version, and no route serves that path. Three things were wrong at once: the URL 404'd, extension/popup.js never read the field (it shows the `how` string), and [`web.py:7090`](../web.py#L7090) `EXT_MIN_VERSION`'s own comment 40 lines above says the extension is side-loaded unpacked with "no update_url" precisely because Chrome can never update it. The field contradicted the design it sat next to.

**Workaround:** Fixed by removing the field rather than adding a page nobody would visit. `how` was already the real answer and was already being displayed.

### B3 — Two test suites could not fail (FIXED 2026-08-20)

scripts/test_visa.py and scripts/test_notify.py printed "N FAILED" and then exited 0. Both run in python-tests.yml under `set -e`, so neither could ever have failed a build. Verified with a forced failure: identical stdout, exit 0 before and exit 1 after.

**Workaround:** Fixed. scripts/run_tests.py also treats a FAILED line as failure regardless of exit code, because the bug class is cheap to keep catching.

### B4 — GitHub Actions cannot reach the database

The Actions DB_PROXY_SECRET has not matched the server's since roughly 2026-08-15, so scheduled runs die on HTTP 401 {"error":"bad signature"}. verify_dates, the analytics rollup and the digest email have not run there since.

**Workaround:** Repost detection is duplicated into bin/cron_scrape.sh and THAT copy works -- do not "clean up the duplicate". Fix by resetting the repository secret to match .db_proxy_secret.

### B5 — Seven employers have no board at all

ASML, Deutsche Bank, LTIMindtree, Marlabs, Qualcomm, Renesas and Tradeweb were reachable only through Adzuna, which was removed on 2026-08-16. Their boards rows still say ats_type="adzuna" and are inert.

**Workaround:** Each needs its real ATS tenant id; guessed Workday and Greenhouse tenants did not resolve. `python scripts/probe_adzuna_replacements.py` walks the detect chain.

## Shadowed definitions

Bound more than once at module level. Python resolves globals at call time, so the **last** binding is the one that runs — even if the code using it sits thousands of lines above. Found automatically, which is how B1 stopped being invisible.

*Rebind* means the second binding consumes the first (`X = tuple(f(p) for p in X)`) — deliberate, nothing lost. *Shadow* means it does not, so the earlier definition is dead code and whoever wrote it did not get what they intended.

| File | Name | Bound at | |
|---|---|---|---|
| `web.py` | `_BOARDS_TTL` | [4138](../web.py#L4138), [5829](../web.py#L5829) | ⚠ **shadow** |
| `resume_bullets.py` | `_METRIC_HINTS` | [75](../resume_bullets.py#L75), [113](../resume_bullets.py#L113) | rebind |

---

Full symbol map: [MAP.md](MAP.md) · How it works: [ARCHITECTURE.md](ARCHITECTURE.md) · How to do things: [OPERATIONS.md](OPERATIONS.md)
