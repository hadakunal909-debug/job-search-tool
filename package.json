{
  "name": "job-search-tool",
  "version": "1.0.0",
  "private": true,
  "description": "Personal job-search tool. This is a PYTHON/Streamlit app — package.json only exists so `npm run dev` works out of habit; the scripts just call Python.",
  "scripts": {
    "dev": "python -m streamlit run app.py",
    "scrape": "python scraper.py",
    "score": "python score_jobs.py",
    "refresh": "python scraper.py && python score_jobs.py"
  }
}
