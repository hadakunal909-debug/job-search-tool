# Repo root: each project keeps its own .gitignore (see "JobMatch Scraper/.gitignore").
# These repo-wide patterns are a safety net so secrets/junk can never land from any folder.
.env
secrets.toml
*.key
__pycache__/
*.pyc
.claude/
.DS_Store
